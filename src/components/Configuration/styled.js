import styled from 'styled-components';
import { Paper } from '@material-ui/core';

export const ConfigurationContainer = styled(Paper)`
    padding: 24px;
    margin-bottom: 14px;
    /* display: flex; */

`;
